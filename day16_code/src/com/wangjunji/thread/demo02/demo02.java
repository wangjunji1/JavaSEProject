package com.wangjunji.thread.demo02;

/**
 * try...catch 异常处理的第二种广东，自已处理
 * 格式：
 *      try{
 *          可能产生异常的代码
 *      }catch（定义一个异常的变量，用来接收）
 *      {
 *      }
 *   注意：try中可能抛出多个异常对象，那么就使用catch来处理这些异常对象
 *   如果try中产生了异常，那么就会执行catch中的异常处理逻辑，执行完毕catch中处理逻辑
 *   继续执行try。。。catch之后的代码
 *   如果try 中没有产生异常，那么就不会执行catch中异常的处理逻辑，执行完try中的代码，继续执行try
 *   catch之后的代码
 */
public class demo02 {
}
